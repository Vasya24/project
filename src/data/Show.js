export default class Show {
  constructor(date, artist, city, place, cancelled) {
    this.date = date;
    this.artist = artist;
    this.city = city;
    this.place = place;
    this.cancelled = cancelled;
  }
}
